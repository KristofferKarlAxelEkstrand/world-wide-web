{
	"name": "world-wide-web",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "npm run start-page && vite",
		"build": "npm run start-page && vite build",
		"format": "prettier --write .",
		"start-page": "node dir.js",
		"watch:scss": "sass src:src --no-source-map --watch"
	},
	"devDependencies": {
		"prettier": "^3.6.2",
		"prettier-plugin-css-order": "^2.1.2",
		"sass": "^1.92.1",
		"vite": "^7.1.5"
	}
}
