{
	"name": "world-wide-web",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "npm run startpage && vite",
		"build": "npm run startpage && vite build",
		"format": "prettier --write .",
		"startpage": "node dir.js"
	},
	"devDependencies": {
		"prettier": "^3.5.3",
		"prettier-plugin-css-order": "^2.1.2",
		"sass-embedded": "^1.89.1",
		"vite": "^6.3.5"
	}
}
