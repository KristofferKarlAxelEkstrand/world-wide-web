{
	"name": "world-wide-web",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "node dir.js && vite",
		"build": "node dir.js && vite build",
		"format": "prettier --write ."
	},
	"devDependencies": {
		"prettier": "^3.3.3",
		"prettier-plugin-css-order": "^2.1.2",
		"vite": "^5.4.10",
		"sass-embedded": "^1.0.0"
	}
}
